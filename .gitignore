# Build artifacts -- large, disposable, regenerated by `cargo build`.
# No leading slash, so this matches `target/` at any depth (root, cli/,
# core/, web/), not just the repository root.
target/
pkg/

# Wallet files -- never commit these.
*.dat

# Vercel CLI project-linking metadata
.vercel

# Environment files
.env
.env.local